const RELATIONSHIPS = (state) => state.relationships;
const RELATIONSHIP = (state) => state.relationship;
const TYPES = (state) => state.types;

exports.module = {
  RELATIONSHIPS,
  RELATIONSHIP,
  TYPES,
};
